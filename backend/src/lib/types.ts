export type signupType = {
  email: string;
  password: string;
  name: string;
  image?: string;
};

export type loginType = {
  email: string;
  password: string;
};

export type response = {
  id: string;
};
